[package]
name = "simulator"
version = "0.1.0"
edition = "2024"

[dependencies]
shared-types = { path = "../shared-types" }
config = { path = "../config" }
anyhow = "1"
tokio = { version = "1", features = ["full"] }
